#!/usr/bin/env bash
set -euo pipefail

echo "[logging-check] BLOCKED: not implemented yet" >&2

echo "Goal: enforce logging/operational invariants (no raw payload logs, consistent request IDs, etc.)." >&2

echo "Implement with: tests + grep-based checks + (optionally) linter rules." >&2

exit 2
